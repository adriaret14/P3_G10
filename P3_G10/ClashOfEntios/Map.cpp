#include "Map.h"



Map::Map(std::string url)
{
	/*std::ifstream archivo;
	std::string linia;


	archivo.open(url);

	if (archivo.fail())
	{
		system("pause");
	}
	else
	{
		while (!archivo.eof())
		{
			std::getline(archivo, linia);
		}
	}


	archivo.close();*/
}

Map::~Map()
{
}
